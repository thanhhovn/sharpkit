/*Generated by SharpKit 5 v5.00.0000*/
$(OnReady);
function OnReady()
{
    $("#selectable").selectable(
    {
        stop: function (e, ui)
        {
            var result = $("#select-result").empty();
            $(".ui-selected", e.currentTarget).each(function (number, elemnt)
            {
                var index = $("#selectable li").index(elemnt);
                result.append(" #" + (index + 1));
            });
        }
    });
};
