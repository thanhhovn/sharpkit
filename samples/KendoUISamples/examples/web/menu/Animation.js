/*Generated by SharpKit 5 v4.29.7000*/
$(OnReady);
function OnReady()
{
    var getEffects=function()
    {
        return (($("#expand")[0]?"expand:vertical ":"") + ($("#slide")[0]?"slideIn:down ":"") + ($("#opacity")[0]?"fadeIn":""));
    };
    var initMenu=function()
    {
        $("#menu").kendoMenu(
        {
            animation:
            {
                open:{effects:getEffects()}
            },
            hoverDelay:$("#delay")[0].valueOf()
        }).css({marginRight:"220px"});
    };
    var original=$("#menu").clone(true);
    original.find(".k-state-active").removeClass("k-state-active");
    $(".configuration input").change(function(e)
    {
        var menu=$("#menu");
        var clone=original.clone(true);
        menu.data("kendoMenu").close($("#menu .k-link"));
        menu.replaceWith(clone);
        initMenu();
    });
    initMenu();
};
