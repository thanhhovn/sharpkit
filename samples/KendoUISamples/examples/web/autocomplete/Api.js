/*Generated by SharpKit 5 v5.00.0000*/
var autoComplete = null;
$(OnReady);
function OnReady()
{
    var data = ["Red-violet", "Red", "Red-orange", "Orange", "Yellow-orange", "Yellow", "Yellow-green", "Green", "Blue-green", "Blue", "Blue-violet", "Violet"];
    $("#products").kendoAutoComplete(data);
    autoComplete = $("#products").data("kendoAutoComplete");
    var setValue = SetValue;
    var setSearch = SetSearch;
    $("#set").click(setValue);
    $("#value").keypress(setValue);
    $("#search").click(setSearch);
    $("#word").keypress(setSearch);
};
function SetValue(e)
{
    if (e.type != "keypress" || kendo.keys.ENTER == e.keyCode)
        autoComplete.value($("#value").val());
};
function SetSearch(e)
{
    if (e.type != "keypress" || kendo.keys.ENTER == e.keyCode)
        autoComplete.search($("#word").val());
};
