/*Generated by SharpKit 5 v4.29.7000*/
$(OnReady);
function OnReady()
{
    var fields=new Object();
    fields["ProductID"] = {editable:false,nullable:true};
    fields["ProductName"] = "ProductName";
    fields["UnitPrice"] = {type:"number"};
    fields["Discontinued"] = {type:"boolean"};
    fields["UnitsInStock"] = {type:"number"};
    var crudServiceBaseUrl="http://demos.kendoui.com/service";
    var dataSource=new kendo.data.DataSource(
    {
        transport:
        {
            read:{url:crudServiceBaseUrl + "/Products/Update",dataType:"jsonp"},
            update:{url:crudServiceBaseUrl + "/Products/Update",dataType:"jsonp"},
            destroy:{url:crudServiceBaseUrl + "/Products/Destroy",dataType:"jsonp"},
            create:{url:crudServiceBaseUrl + "/Products/Create",data:"jsonp"},
            parameterMap:function(options,operation)
            {
                if(operation !== "read" && options["models"])
                {
                     return {models: kendo.stringify(options.models)};;
                    return null;
                }
                return null;
            }
        },
        batch:true,
        pageSize:4,
        schema:
        {
            model:{id:"ProductID",fields:fields}
        }
    });
    $("#pager").kendoPager({dataSource:dataSource});
    var listView=$("#listView").kendoListView({dataSource:dataSource,template:kendo.template($("#template").html()),editTemplate:kendo.template($("#editTemplate").html())}).data("kendoListView");
    $(".k-add-button").click(function(e)
    {
        listView.add();
        e.preventDefault();
    });
};
