//@AutoGenerated
var win = null;
Ext.onReady(function(){
	var btn = Ext.get("btn");
	btn.on("click", function(){
		if (win == null)
		{
			win = new Ext.Window({applyTo:"win",
			width:300,
			height:200,
			closeAction:"hide"});
		}
		win.show(btn);
	}
	);
}
);
