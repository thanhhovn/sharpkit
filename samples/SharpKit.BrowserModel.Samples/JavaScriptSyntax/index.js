//@AutoGenerated

ShoppingCart = function()
{
}
ShoppingCart.prototype.total = 0;

ShoppingCart.prototype.items = new Array();

ShoppingCart.prototype.addItem=function ShoppingCart$addItem(name, price)
{
	var p = {name:name,
	price:price};
	this.items.push(p);
	this.total += price;
}

ShoppingCart.prototype.print=function ShoppingCart$print(writer)
{
	writer.write("total products:" + this.items.length);
	writer.write(", total price:" + this.total);
}
function anonymous_function_demo()
{
	var f = function(){
		document.writeln("anonymous function demo: this line was emitted from an anonymous function<br/>");
	}
	;
	f();
}
function json_syntax_demo()
{
	var p = {name:"Bread",
	price:10};
	document.writeln("json demo: a new Product was created - {name:" + p.name + ", price:" + p.price + "}<br/>");
}
function prototype_demo()
{
	var cart = new ShoppingCart();
	document.writeln("prototype demo: a new Cart was created - ");
	cart.addItem("Milk", 9);
	cart.addItem("Toothpaste", 22);
	cart.print(document);
}
document.writeln("global code demo: this line was emitted from global code<br/>");
anonymous_function_demo();
json_syntax_demo();
prototype_demo();
