/*Generated by SharpKit 5 v5.00.2000*/
function OnReady()
{
    var http = require("http");
    var mysql = require("mysql");
    var connection = mysql.createConnection( {user: "root", password: "", database: "db_name"});
    http.createServer(function (request, response)
    {
        request.on$$String$$JsAction("end", function ()
        {
            connection.query("SELECT * FROM your_table;", function (error, rows, fields)
            {
                response.writeHead(200, {ContentType: "x-application/json"});
                response.end(JSON.stringify(rows));
            });
        });
    }).listen(8080);
};
function require(className)
{
    return null;
};
